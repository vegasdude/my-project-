import argparse
from .core import greet

def main():
    parser = argparse.ArgumentParser(description="My CLI App")
    parser.add_argument("name", help="Anthony Dudek")
    args = parser.parse_args()

    print(greet(args.name))

if __name__ == "__main__":
    main()